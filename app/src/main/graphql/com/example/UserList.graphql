query UserList($username: String!) {
    stratz {
        search(request: {
            query : $username
            searchType : [PLAYERS]
            take : 100
        }) {
            players {
                id
                avatar
                name
                seasonRank
                lastMatchDateTime
            }
        }
    }
}
