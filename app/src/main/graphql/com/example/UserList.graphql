query UserList($username: String! = "whitelext") {
    stratz {
        search(request: {
            query : $username
            searchType : [PLAYERS]
            take : 400
        }) {
            players {
                id
                avatar
                name
                seasonRank
                lastMatchDateTime
            }
        }
    }
}
